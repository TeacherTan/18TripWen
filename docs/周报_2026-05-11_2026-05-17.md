# 周报（2026-05-11 - 2026-05-17）

> 目标受众：个人及直属上级。用于每周工作自省与汇报。

---

## 一、里程碑进度

### 1. 本周关键产出

**系统搭建 / 版本更迭**

- **Phase 1 完成**（commit `152a812`）：NFC 认证系统的后端 + 前端骨架
  - 后端：Express JWT 认证体系（`server/routes/auth.js`：NFC 登录/注册/密码登录/刷新/me；`server/middleware/authenticate.js`：Bearer JWT 验证；`server/utils/jwt.js`：token 签发与校验；`server/db/schema.sql`：users 表设计）
  - 前端：React 19 + Vite 认证页面骨架（`src/pages/Login.jsx`、`Register.jsx`、`Welcome.jsx`；`src/hooks/useNfcLogin.js`：检测 `?nfc=TOKEN` 自动登录；`src/context/AuthContext.jsx`：全局会话状态；`src/styles/auth.css`：暗色主题）
  - 基础设施：`docker-compose.yml`（PostgreSQL 16）、`vite.config.js`（/api 代理）、`server/db/index.js`（pg Pool）

- **Phase 2 完成**（commit `56cf3dd`）：打卡接口与成就墙
  - 后端：`server/routes/checkin.js`（POST /api/check-in 打卡、GET /api/check-in/status 成就墙状态、GET /api/check-in/spots 打卡点列表）；幂等打卡设计（`UNIQUE(user_id, spot_id)`）
  - 前端：`src/pages/CheckIn.jsx`（打卡结果页）、`src/pages/Profile.jsx`（成就墙 6 格进度）、`src/components/AchievementSlot/`（单个成就槽组件，支持 locked/unlocked/highlighted 状态 + unlock-pulse 动画）、`src/styles/achievement.css`
  - 5 月 17 日追加优化（commit `cc3637d`）：打卡后跳转成就页弹窗展示结果，浓缩已实现文档

- **Phase 3 完成**（commit `caefef3`）：后台管理系统 + 测试材料
  - 后端：`server/routes/admin.js`（12 个管理接口：用户 CRUD / 停用 / 挂失重发 / 数据迁移；打卡点 CRUD；打卡记录查询 / 补卡）；`server/middleware/requireAdmin.js`（角色守卫）
  - 前端：`src/pages/admin/AdminLayout.jsx`、`AdminUsers.jsx`、`AdminSpots.jsx`、`AdminCheckIns.jsx`；`src/styles/admin.css`
  - 测试材料：`docs/testing.md`（32 个测试用例 + 环境配置 + 移动端真机测试指南）、`server/api.http`（23 个 VS Code REST Client 联调请求）

**文档产出**

- `README.md` 重写：项目概述、功能概览、技术栈、NFC 写卡说明
- `CLAUDE.md` 新建：项目总览、目录结构、数据模型、关键设计决策、环境变量、NPM 脚本
- `docs/superpowers/specs/` 设计文档完善：NFC 方案设计、打卡成就 PRD、后台管理 PRD、部署方案
- `docs/testing.md`：完整测试手册

### 2. 重大变动记录

- **技术栈确定**：React 19 + Vite / Express 5 / PostgreSQL 16 / Docker
- **NFC 交互方案变更**：放弃 Web NFC API，改用 URL 参数 `?nfc=` / `?spot=`，兼容 iOS Safari
- **成就系统架构决策**：从 `check_ins × check_in_spots` 连表推导，无需独立 achievements 表
- **文件存储策略**：本地磁盘 + adapter 模式（`server/storage/`），保留切换阿里云 OSS 的能力
- **打卡结果展示方式变更**：从独立 `/checkin` 页面改为跳转 `/profile` 以弹窗形式展示（`cc3637d`）

---

## 二、风险与阻塞

### 风险项

- **设计师素材交付延迟**：6 张成就墙图纸（`spot_01`~`spot_06_unlocked.png`）+ 6 个黑色轮廓占位 SVG（`spot_01`~`spot_06_locked.svg`）尚未交付。影响成就墙最终视觉效果和真机测试体验。概率：高（外部依赖）。
- **iOS PWA NFC 体验限制**：iOS 碰 NFC 默认打开 Safari，不会自动跳转到已安装的 PWA。设计层面已接受此限制，但需在用户手册中明确提示。

### 阻塞项

- **云服务器尚未开通**：部署方案（`docs/superpowers/specs/features/deployment.md`）已定，但服务器未采购，域名 `tyzhome.xyz` 已确认但未解析。阻塞生产环境部署。
- **头像上传 UI + 接口未实现**：存储适配器已就绪，但前端上传组件和 `/api/upload/avatar` 接口未开发，等设计稿确认交互。

---

## 三、自省与回顾

### 1. 上周计划执行回顾

- 无历史参考（本周为首次生成周报）。

### 2. 改进项

- **Phase 1-3 代码密度高，但 commit 粒度偏大**：`152a812`、`56cf3dd`、`caefef3` 三个 commit 各自完成了一个完整 Phase，单次提交涉及文件过多（Phase 1 涉及 20+ 文件）。后续迭代建议按功能模块拆分 commit，便于回滚和 code review。
- **文档与代码同步存在滞后**：`cc3637d` 中对打卡跳转逻辑进行了代码级调整（不再跳转 `/checkin`），设计文档（`checkin-achievement.md`）在当天同步更新，但前期存在"先写代码后补文档"的情况。建议核心逻辑变更时文档与代码同提交。

---

## 四、下周计划与依赖

### 计划

1. **生产环境部署**：开通云服务器（最低 1C1G Ubuntu 22.04），配置 Nginx 反向代理 + Let's Encrypt SSL + PM2 进程管理，域名 `tyzhome.xyz` 解析。（优先级：P0）
2. **头像上传功能**：实现 `/api/upload/avatar` 接口（multipart/form-data，复用 storage adapter）+ 前端头像上传 UI。（优先级：P1，等设计稿）
3. **设计师素材接入**：将 6 张图纸和占位 SVG 放入 `src/assets/achievements/` 目录，验证 `AchievementSlot` 组件资源加载路径。（优先级：P1，等设计交付）
4. **端到端真机测试**：使用 NFC Tools + NTAG213 芯片，在 iOS/Android 真机上完成 32 个测试用例的全流程验证。（优先级：P1）

### 依赖

- **设计师**：6 张图纸 + 6 个 SVG 占位图，预计交付时间待确认。
- **运维/采购**：云服务器开通及域名解析权限，预计 1-2 个工作日。
